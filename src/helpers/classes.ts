import type { Data } from '@/types/generic';

const wrapperClasses: Data = {
  AppColGroup: 'constrain mx-auto border-2 border-black p-5 lg:p-16',
  AppPBlock: 'xl:flex',
};

export { wrapperClasses };
